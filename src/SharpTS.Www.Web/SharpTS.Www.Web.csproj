<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <!-- English strings live in the neutral (unsuffixed) .resx files, so no satellite
         assembly is needed for the default language. -->
    <NeutralResourcesLanguage>en</NeutralResourcesLanguage>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\SharpTS.Www.ServiceDefaults\SharpTS.Www.ServiceDefaults.csproj" />
  </ItemGroup>

</Project>
